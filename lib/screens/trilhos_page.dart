import 'package:flutter/material.dart';

class TrilhosPage extends StatelessWidget {
  const TrilhosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Trilhos"),
    );
  }
}
